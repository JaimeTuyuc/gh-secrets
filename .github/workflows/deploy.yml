name: Deployment
on:
  push:
    branches:
      - master
      - dev
env:
    MONGODB_DB_NAME: 'gh-demo'
jobs:
  test:
    env:
      MONGODB_CLUSTER_ADDRESS: 'nextjscourse.xnwca09.mongodb.net'
      MONGODB_USERNAME: 'jtuyuc7'
      MONGODB_PASSWORD: 'Si0ZTpKtIP7hU2XK'
      PORT: 8080
      # MONGODB_DB_NAME: 'test-database'
    environment: testing
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3
      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: npm-deps-${{ hashFiles('**/package-lock.json') }}
      - name: Install dependencies
        run: npm ci
      - name: Run server
        run: npm start & npx wait-on http://127.0.0.1:${{ env.PORT }}
      - name: Run tests
        run: npm test
      - name: Output information
        run: |  
          echo "..."
          echo "MONGODB_CLUSTER_ADDRESS: ${{ env.MONGODB_CLUSTER_ADDRESS }}"
          echo "MONGODB_USERNAME: ${{ env.MONGODB_USERNAME }}"
          echo "MONGODB_PASSWORD: ${{ env.MONGODB_PASSWORD }}"
          echo "PORT: ${{ env.PORT }}"
          echo "MONGODB_DB_NAME: ${{ env.MONGODB_DB_NAME }}
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: |        
          echo "..."
